embedding_size: 64              # (int) The embedding size of users and items.
order: 2                        # (int) The order of proximity of the model.
second_order_loss_weight: 1.0   # (float) The weight of the second proximity loss.