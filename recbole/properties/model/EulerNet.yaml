embedding_size: 16              # (int) The embedding size of features.
order_list: [30]                # (list) The order vectors of EulerNet
drop_ex: 0.3                    # (float) The dropout rate for the modulus and phase
drop_im: 0.3                    # (float) The dropout rate for the real and imaginary part 
apply_norm: False               # (bool) Whether perform the layer norm
reg_weight: 1e-5                # (float) The L2 regularization weight.